# AI顶刊顶会研究全景平台｜对标 Artificial Analysis 改版方案

整体对标 `artificialanalysis.ai` 的设计逻辑：**以指数化、全景化、对比化的高视角呈现 AI 学术版图**，核心做「宏观研究趋势洞察」而非「单篇论文检索工具」；同时补充完整的会议元信息（投稿、截稿、会期），做成「AI 学术研究仪表盘 + 顶会日程信息库」二合一的专业平台。

> 修订记录（2026-08-10）：基于对标站点抓取 + 数据源核查，对原稿做三处实质修正 ——
> (1) 数据源部分：替换已关停的 Papers with Code / 停更的 AMiner，改用 OpenAlex（CC0）+ ccfddl YAML；
> (2) 新增「八、收录范围与 CCF 分级全清单（含截止日期字段）」并引用 LIST.md 作为白名单来源；
> (3) 指数方法论补充「可复现性 / 开源率口径」与局限性；并新增「七、研究方向里程碑与前沿瓶颈」功能。

---

## 一、项目核心定位
参考 Artificial Analysis 做模型能力指数的思路，我们打造 **AI Research Landscape Index（AI 研究版图指数）**：
- 视角：站在领域全局，汇总 CCF A/B/C 类全部 AI 顶会顶刊（并延伸至数据库/图形多媒体/安全/系统/人机交互等 AI 研究者高频投稿的相邻领域），输出宏观趋势、方向热度、会议画像、历年演化。
- 核心载体：**指数 + 图表 + 对比**，默认不堆砌单篇论文，论文列表仅作为数据下钻的末端入口。
- 附加价值：标准化的顶会时间线与投稿信息（**开始投稿日期 / 摘要截稿 / 全文截稿 / 通知日期 / 开会日期**），解决科研人员「查截稿、看会期、选会议」的刚需。
- 视觉风格：极简学术风，深色/浅色双模式，模块化仪表盘，数据可视化为主，文字说明为辅。

## 二、核心展示指标体系
全部指标以**聚合统计值**呈现，不做单篇论文维度的信息罗列，分为四大类：

### 1. 全局综合指数（首页核心）
- **AI Research Heat Index 综合热度指数**：全领域年度整体科研活跃度综合分。
- 领域总产出：当年顶会顶刊录用论文总数、同比增长率。
- 方向分布占比：Top 10 研究方向的论文占比。
- 开源率、平均引用中位数等全领域基准值。
- 年度新兴方向标签、衰退方向标签。

### 2. 研究方向维度指标
每个研究方向对应一张独立画像页，核心指标：
- 方向热度指数（综合分）及历年走势。
- 年度论文产出量、3 年复合增长率。
- 平均引用量、高被引论文占比（Top 10%）。
- 核心承载会议：该方向论文主要发表在哪些顶会。
- 核心产出机构/学者 TOP10。
- 交叉关联度：与其他研究方向的交叉融合程度。

### 3. 会议/期刊维度指标
每个会议/期刊对应一张独立画像页，核心指标：
- 会议基础等级：**CCF 分类（A/B/C）+ CCF 领域、CORE Rank、THCPL 等级、是否 EI/SCI 检索**。
- 历年录用规模：总投稿数、录用数、录用率及走势（期刊用「年发文量 + 影响因子走势」近似）。
- 研究方向分布：该会议各方向论文占比，以及历年变化。
- 论文平均引用、高被引论文占比（衡量会议学术影响力）。
- 机构/学者发文榜。
- 会议基本信息：全称、简称、举办周期、官网链接。

### 4. 时间演化维度指标
- 历年方向热度排行变化。
- 历年会议影响力排行变化。
- 各方向的崛起/峰值/衰退时间节点。
- 新兴方向识别：连续 2 年高增长的细分领域。

---

## 三、数据来源与更新机制

### 1. 论文统计数据（用于热度/趋势计算）
优先采用权威公开数据源，保证统计口径一致，避免逐篇爬取：
1. **DBLP**（ODC-BY，无需鉴权）：作为基准论文库，按 `dblp.org/db/conf` 与 `/db/journals` 获取所有目标会议/期刊的正式录用论文列表，保证「只统计正式发表、不混入预印本」，最适合做 venue 维度的权威计数。
2. **OpenAlex**（CC0）：替代原 AMiner 角色。用其 `concepts` 主题层级（含 AI/CV/NLP/ML 等）做研究方向自动分类；用 `citation_normalized_percentile` 近似 FWCI 做影响力归一化；可走 `api.openalex.org`（2026 起需免费 `api_key`）或 `s3://openalex` 全量快照离线建库。
3. **Semantic Scholar API / Datasets**（ODC-BY，需署名 semanticscholar.org）：批量获取精确引用量、作者、机构信息，用于高被引占比与机构榜。匿名共享池限流，建议申请 API key（默认 1 req/s，可提额）；全量数据集桶 `ai2-s2ag`。
4. **OpenReview API v2**（`api2.openreview.net`）：获取 NeurIPS / ICLR / ICML 等会议投稿、审稿、录用结果、接收等级（Oral/Spotlight/Poster），用于会议录用率与接收分布。
5. **arXiv API**（`export.arxiv.org`，元数据 CC0）：预印本补充与「录用前」趋势探测（cs.AI/LG/CV/CL/RO、stat.ML），但**热度统计以 DBLP 正式发表为准**，arXiv 仅作前瞻指标。

> 已剔除：原方案的 **AMiner 开放数据集**（已多年未稳定更新）、**Papers with Code**（2025-07 由 Meta 关停，API/站点不再维护；历史 dump 见 `github.com/paperswithcode/paperswithcode-data`）。

### 2. 会议元数据（投稿/截稿/会期 + 等级标签）
1. **ccfddl/ccf-deadlines**（GitHub，YAML）——**主数据源**。其 `conference-yaml-schema.yml` 已内嵌 `sub`（ccf/core/thcpl 等级）、`dblp` 链接、`confs[]`（含 `id`/`link`/`timezone`/`place` + `timeline[abstract_deadline, deadline, comment]` + `date`），天然满足「开始投稿日期 + 截稿日期 + 开会日期」的结构化需求，且自带 CCF 标签。
   - 单会议结构化字段（入库规范）：`title` / `sub`（等级） / `dblp`（venue 键） / `confs[].year` / `abstract_deadline`（开始投稿/摘要截稿） / `deadline`（全文截稿） / `notification`（通知） / `date`（开会日期） / `place`（地点） / `timezone` / `link`（官网）。
2. **Hugging Face `ai-deadlines`**（原 `paperswithcode/ai-deadlines` 继任者，MIT，集成 `hf.co/papers`）：补充最新截稿动态与社区维护的截止日期。
3. **WikiCFP**：CFP 文本兜底来源（无官方 API，需 RSS/爬虫），用于校验与补漏。
4. **CCF 推荐目录**（2022 版 / 第七版 2026）：通过 **CCFrank4dblp** 等机器可读映射获取 CCF A/B/C 等级 + 领域，并与 DBLP venue 名交叉校验。

### 3. 更新节奏
- 会议元信息：以 ccfddl YAML 为准，提前 3-6 个月更新下一届投稿、截稿、会期，支持订阅/截稿提醒。
- 论文统计数据：会议结束后 1-2 个月经 DBLP 批量拉取当年录用列表，全年按月对引用数据做增量刷新（OpenAlex / Semantic Scholar）。
- 热度指数：每季度重新计算发布一次，同步更新版本号（参考 AA 的 Intelligence Index 版本机制）。

### 4. 研究方向分类体系
不做人工拍脑袋标签，采用「**OpenAlex concepts 层级 + DBLP 会议领域**」双层映射：
- 一级大类：CV、NLP、ML/深度学习、机器人、多模态、数据挖掘/IR、AI 理论/博弈、安全/隐私、人机交互、AI4Science 等；
- 每个方向绑定对应的 CCF 会议集合（如 CV 绑定 CVPR/ICCV/ECCV/WACV/ACCV/BMVC），保证「方向热度」与「会议录用」口径一致、可互相下钻。

---

## 四、核心评估方法论（对标 AA 的指数设计）
参考 Artificial Analysis 的多维度加权指数思路，设计 **AI Research Heat Index（研究热度指数）**，全程公开方法论，保证可解释、可复现。

### 1. 指数构成（加权综合）
\[
\text{Heat Index} = w_1 \cdot \text{产出规模分} + w_2 \cdot \text{学术影响力分} + w_3 \cdot \text{增长趋势分} + w_4 \cdot \text{生态活跃度分}
\]
| 维度 | 计算方式 | 默认权重 |
|------|----------|----------|
| 产出规模分 | 该方向当年顶会顶刊论文数量归一化 | 40% |
| 学术影响力分 | 论文平均引用、高被引占比（Top 10%）加权归一化 | 30% |
| 增长趋势分 | 近 2 年论文数量复合增长率归一化 | 20% |
| 生态活跃度分 | 论文开源率（GitHub 链接占比，默认口径）、代码仓库星标数（可选增强，不计入默认权重） | 10% |

### 2. 多视角切换（参考 AA 的多榜单设计）
- **产出榜**：仅按论文数量排序，看领域规模。
- **影响力榜**：仅按引用/高被引排序，看学术含金量。
- **新兴趋势榜**：仅按增长率排序，挖掘潜力方向。
- **会议适配榜**：按方向匹配最适合投稿的顶会。

### 3. 方法论透明化
单独设置 **Methodology** 页面，完整公开：
- 纳入统计的会议/期刊清单与筛选标准（链接到第八节 + LIST.md）。
- 研究方向分类体系与标注规则。
- 热度指数计算公式、权重设定、归一化方法、每次重建的数据快照哈希（可复现性）。
- 数据来源与更新频率。
- 局限性说明（新论文引用滞后、分类误差、开源率依赖 GitHub 链接识别、新方向冷启动、CCF 等级年度变动导致的跨年可比性）。

---

## 五、网站整体架构与多维展示
参考 AA 的顶部导航 + 模块化卡片布局，核心页面如下，全程贯彻「宏观优先、下钻细化」的逻辑：

### 1. 首页：全局研究仪表盘
- 顶部：AI Research Heat Index 综合指数总览，同比变化，当前版本号。
- 核心模块1：研究方向热度总榜（Top 15），支持切换「综合/产出/影响力/新兴」四种排序。
- 核心模块2：全年顶会时间轴，标注即将截稿、正在投稿、即将召开的会议（使用第八节截止日期字段）。
- 核心模块3：领域趋势大图（堆叠面积图，历年各方向论文占比演化）。
- 底部模块：近期更新日志、新增会议信息。

### 2. 研究方向版图页
- 顶部：方向筛选栏，支持按大类（CV/NLP/ML/机器人/多模态等）过滤。
- 主视图：气泡象限图（X 轴=增长率，Y 轴=影响力，气泡大小=论文数量）。
- 下方：方向排行榜表格，点击任意方向进入详情页。
- 详情页：该方向历年热度曲线、雷达画像、核心会议分布、代表机构、交叉方向；并新增「里程碑」「瓶颈」两个 Tab（见第七节）。

### 3. 会议全景页
- 顶部：按 CCF 分类、领域（CV/NLP/ML 等）筛选。
- 主视图：会议影响力排行榜，展示录用率、平均引用、热度分。
- 点击单会议进入详情页：
   - 会议基础信息卡（CCF 等级、CORE、官网、举办地/线上）。
   - **投稿时间线**：开始投稿（摘要截稿）、全文截稿、通知时间、开会时间（来自第八节结构化字段）。
   - 历年录用数据走势（投稿数、录用数、录用率）。
   - 研究方向分布桑基图。
   - 历年高影响力论文 TOP10（仅作代表展示，不做全量列表）。

### 4. 会议日历页
专门解决投稿时间需求：
- **年历视图**：全年 12 个月标注所有顶会的截稿日、开会日，颜色区分 CCF 等级。
- **时间线视图**：按时间顺序列出未来 6 个月的重要节点（开始投稿 → 截稿 → 通知 → 开会）。
- 筛选功能：只看 A 类、只看 CV 领域、只看线下会议等。
- 支持一键添加到日历（.ics）、设置截稿提醒。

### 5. 对比分析页
- 支持 2-5 个研究方向对比：热度曲线、增长率、影响力等指标并排展示。
- 支持 2-5 个会议对比：录用率、难度、方向偏向、影响力对比。
- 可对比两个方向的瓶颈重叠度，发现交叉难题（见第七节）。
- 输出对比雷达图、折线对比图、指标对比表。

### 6. 方法论 & 更新日志页
- Methodology：完整公开指数算法、数据口径、分类标准。
- Changelog：按版本号记录每次数据更新、新增会议、算法调整。

---

## 六、核心可视化图表设计
延续 AA 的简洁专业风格，全部采用交互式图表，hover 显示详情，点击支持下钻：

### 1. 全局趋势类
- **堆叠面积图**：历年各研究方向论文数量/占比演化（首页核心大图）。
- **热力矩阵图**：行=研究方向，列=年份，颜色深浅=热度指数，一眼看全领域兴衰。

### 2. 对比画像类
- **四象限气泡图**：X 轴增长率、Y 轴影响力、气泡大小产出规模，自动标注「明星/成熟/潜力/衰退」方向。
- **雷达图**：单个方向/单个会议的多维画像，覆盖产出、影响力、增长、生态等维度。
- **分组条形图**：会议影响力排行、方向热度排行，支持切换排序指标。

### 3. 结构分布类
- **桑基图**：展示「会议 → 研究方向 → 机构」的流向，看清各会议的方向侧重。
- **河流图**：美化版堆叠面积图，用于展示多个细分方向的此起彼伏。
- **环形饼图**：单个会议/单个年份的研究方向占比分布。

### 4. 时间节点类
- **横向时间轴**：单会议投稿全流程节点（开始投稿 → 截稿 → 审稿 → 通知 → 开会）。
- **年度日历热力图**：标注全年顶会截稿与召开日期。

---

## 七、研究方向里程碑与前沿瓶颈（新增功能）
除宏观热度与会议画像外，为每个研究方向提供「发展脉络」与「待解问题」两张视图，对标 AA 的 Capability Indices 思路，把抽象方向具象为可追溯的知识结构。

### 1. 里程碑工作树状图（Milestone Tree）
- 目的：梳理每个方向的重要里程碑工作，画出树状/有向无环图，标注领域发展路径。
- 数据：以 Semantic Scholar 高被引 + 顶会最佳论文 + 综述引用频次筛选里程碑；用 OpenAlex `concepts` 共引聚类与引用关系推断 `parent_ids`，构建继承树（如 CV：LeNet→AlexNet→ResNet→ViT→多模态）。
- 可视化：可折叠**树状图 / 时间树**（根=领域起点，分支=范式转折，叶=最新 SOTA），节点 hover 显示影响力证据，点击下钻到论文/会议页。
- 交互：支持按方向切换、按年份缩放、高亮某条技术路线。

### 2. 前沿瓶颈清单（Frontier Bottleneck List）
- 目的：将每个方向最前沿的瓶颈整理成列表清单，形成「领域待解问题看板」。
- 数据：对近 2 年顶会 survey 的 "Challenges / Limitations / Future Work" 段落做 NLP 抽取，结合专家标注去重归并；每条标注 `status`（未解决/部分解决/已突破）与 `priority`。
- 可视化：**可筛选/可勾选清单或看板**，支持按方向过滤、按状态分组、社区投票更新状态；点击瓶颈可关联到相关里程碑节点与近期相关论文。
- 价值：帮助科研人员快速定位「该往哪发力」，也作为基金/综述写作的痛点参考。

### 3. 与既有模块的衔接
- 入口：研究方向版图页（五-2）每个方向卡片新增「里程碑」「瓶颈」两个 Tab。
- 对比分析页（五-5）可对比两个方向的瓶颈重叠度，发现交叉难题。
- 数据源复用第三节的 OpenAlex / Semantic Scholar；瓶颈语料补充 arXiv survey 与 OpenReview 论坛。
- 元数据结构与字段规范详见 LIST.md 第四节。

---

## 八、收录范围与 CCF 分级全清单（含截止日期字段）
> 白名单以 **`LIST.md`** 为权威来源（已结构化分级、领域标签与元信息字段）。以下为范围说明与字段规范。

### 8.1 收录范围分层
- **① 核心 AI（CCF 人工智能领域 A/B/C）**：AAAI/NeurIPS/ICML/CVPR/ICCV/ACL/IJCAI + COLT/EMNLP/ECAI/ECCV/ICRA/ICAPS/COLING/KR/UAI/AAMAS/PPSN/NAACL + C 类（AISTATS/ACCV/ACML/BMVC/CoNLL/NLPCC/ICANN/FG/ICDAR/KSEM/PRICAI/ICTAI/IJCNN/EACL/GECCO/ICONIP 等）；期刊 TPAMI/IJCV/JMLR/AI + TACL/PR/TNNLS/TFS/TEC/TCYB/TASLP/TAC/JAIR/ML + PRL/Neurocomputing 等。
- **② 业界公认顶会（非 CCF 仍纳入）**：ICLR、CoRL、MLSys、WACV。
- **③ 相邻高频领域（AI 研究者常投）**：KDD/SIGIR/WWW（数据库）、SIGGRAPH/ACM MM（图形多媒体）、S&P/CCS（安全）、OSDI/SOSP/MLSys（系统）、CHI/UIST（人机交互）；Nature/Science/PNAS 作顶刊影响力单列、不参与 CCF 等级聚合。
- 白名单随项目迭代从 A/B 类扩展到全量 C 类。

### 8.2 截止日期字段采集优先级
1. ccfddl YAML（`abstract_deadline` / `deadline` / `date`，已含 timezone、place、link）→ 直接入库。
2. HF `ai-deadlines` → 补漏与最新一届校正。
3. 各会议官网 Call for Papers → 权威兜底。
4. WikiCFP → 文本校验。

### 8.3 标注规则
- 每个 venue 必须带至少一个分类标签：**CCF(A/B/C)** 或 **非 CCF**，并尽量补 **CORE Rank** 与 **THCPL**；期刊额外标 **SCI/EI/影响因子档位**。
- 用 DBLP venue key 作为唯一主键，避免同名歧义；与 CCFrank4dblp 映射做等级交叉校验。
- 每个 venue 维护结构化时间节点：开始投稿(摘要截稿) / 全文截稿 / 通知 / 开会 / 地点 / 时区（详见 LIST.md 第三节）。
